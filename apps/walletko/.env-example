DATABASE_URL="postgresql://user:user@localhost:5433/walletko-dev"
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER="not.important@example.com"
SMTP_PASS="example"
SMTP_SECURE=false
EMAIL_FROM="Walletko <noreply@walletko.app>"
# Generate your own — never reuse this file's value: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-me-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000
APP_URL=http://localhost:3000
